# Public Story API JavaScript Client Example

### Quick Run
- Add your private key, `private_key.pem`, in this folder
- Run the example
```bash
# Install dependencies
npm install
# Run Example
## Update your `iss` and `kid` in the `config.js` file
npm run example
```
